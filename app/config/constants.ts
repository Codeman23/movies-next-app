const accentColor = '#E30B13';
const bgColor = '#191B1F';
const IS_SERVER = typeof window === 'undefined';
const IS_CLIENT = typeof window === 'undefined';

export { accentColor, bgColor, IS_CLIENT, IS_SERVER };
